add_executable(su su.c)
if(SHADOW_PASSWORDS)
    target_compile_definitions(su PRIVATE SHADOW_PASSWORDS)
endif()
target_link_libraries(su crypt)

if(ENABLE_HNAMES)
    set_target_properties(su PROPERTIES PREFIX "h")
endif()
install(TARGETS su)